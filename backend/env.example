# Storage directory for FAISS index and chunks
STORAGE_DIR=./storage

# Embedding model to use (sentence-transformers model name)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Whether to use Google Gemini for answer synthesis (true/false)
USE_GEMINI=false

# Google Gemini API key (required if USE_GEMINI=true)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
