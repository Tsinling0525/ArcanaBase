# Optional: enable Gemini LLM for abstractive answers (otherwise extractive mode)
GEMINI_API_KEY=
USE_GEMINI=false
# Storage folder
STORAGE_DIR=./storage
# Embedding model (defaults to all-MiniLM-L6-v2)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2